[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-talking-photo"
version = "0.1.0"
description = "Create a talking-photo video from a portrait and Traditional Chinese script."
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
